Plugin for CudaLint.
It adds support for CSS lexer.

**** Needs Node.js in PATH - for Win32 it's "node.exe", for Linux it's "nodejs" 
It uses CSSLint tool, included in this package, it's installed via "npm install csslint".

Author: Alexey (Synwrite)
License MIT
